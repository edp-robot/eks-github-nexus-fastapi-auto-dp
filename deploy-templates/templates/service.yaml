apiVersion: v1
kind: Service
metadata:
  name: {{ include "fastapi-auto-dp.fullname" . }}
  labels:
    {{- include "fastapi-auto-dp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "fastapi-auto-dp.selectorLabels" . | nindent 4 }}
